name: proxy-backoff

services:
  app:
    build:
      context: .
      args:
        FLAG: "${FLAG:?set FLAG in challenge/.env}"
    container_name: proxy-backoff-app
    ports:
      - "8094:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped
